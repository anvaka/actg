module.exports = {
  /**
   * Where the data files live
   */
  endpoint:  'http://localhost:9003/out.count.5',

  /**
   * Length of a bounding box for single slice. The larger amount of datapoints
   * you have, the higher this value should be
   */
  boxSize: 1024
};
